def query(id):
    
    #poll the query interface continually until there is an answer of complete or error
    done = False
    while(not done):
        
        print 'query the workflow blackbox'
        
        done = True
                     
    